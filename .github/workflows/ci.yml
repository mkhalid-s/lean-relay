name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel in-progress runs on the same branch/PR when new commits are pushed.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # ── 1) Static checks: syntax, shellcheck, py_compile, help/completions ────
  lint:
    name: Lint (shellcheck + syntax + Python compile)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Bash syntax check
        run: |
          bash -n install.sh
          bash -n bootstrap.sh
          bash -n get.sh
          bash -n bin/apx
          bash -n bin/apx-squeezr
          bash -n build/pack.sh
          bash -n build/header.sh

      - name: Shellcheck
        run: |
          shellcheck --severity=warning \
            install.sh \
            bootstrap.sh \
            get.sh \
            bin/apx \
            bin/apx-squeezr \
            build/pack.sh \
            build/header.sh

      - name: Python compile
        run: python -m py_compile bin/apx-gateway

      - name: Verify apx exports 0.4 gateway config
        run: |
          fake_home="$RUNNER_TEMP/apx-env-home"
          rm -rf "$fake_home"
          mkdir -p "$fake_home/.config/apx" "$fake_home/.local/state/apx" "$RUNNER_TEMP/apx-env-bin"
          cat > "$RUNNER_TEMP/apx-env-bin/fake-gateway" <<'SH'
          #!/usr/bin/env bash
          env | sort > "$HOME/gateway.env"
          python - <<'PY'
          from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
          import os
          class H(BaseHTTPRequestHandler):
              def do_GET(self):
                  if self.path in ("/livez", "/readyz", "/health"):
                      self.send_response(200)
                      self.end_headers()
                      self.wfile.write(b"ok")
                  else:
                      self.send_response(404)
                      self.end_headers()
              def log_message(self, *_args):
                  pass
          ThreadingHTTPServer(("127.0.0.1", int(os.environ["GATEWAY_PORT"])), H).serve_forever()
          PY
          SH
          chmod +x "$RUNNER_TEMP/apx-env-bin/fake-gateway"
          cat > "$fake_home/.config/apx/config.env" <<EOF
          BIND_HOST=127.0.0.1
          GATEWAY_PORT=18787
          PXPIPE_PORT=18788
          HEADROOM_PORT=18789
          SQUEEZR_PORT=18790
          GATEWAY_ENABLED=1
          PXPIPE_ENABLED=0
          HEADROOM_ENABLED=0
          SQUEEZR_ENABLED=0
          GATEWAY_CMD="$RUNNER_TEMP/apx-env-bin/fake-gateway"
          GATEWAY_TARGET_API_URL="https://api.anthropic.com"
          HEALTH_INTERVAL_SECONDS=30
          APX_DASHBOARD_ENABLED=1
          APX_DASHBOARD_TOKEN="secret-token"
          APX_HISTORY_SIZE=123
          APX_METRICS_ENABLED=1
          APX_CAPTURE=full
          APX_CAPTURE_FULL_ACK=i-understand
          APX_CAPTURE_BODY_CAP=777
          APX_REDACT_KEYS=internal_secret
          APX_METRICS_DB="$fake_home/.local/state/apx/custom.db"
          APX_METRICS_RETENTION_DAYS=9
          APX_METRICS_BACKFILL=0
          APX_MAX_REQUEST_BYTES=888
          EOF
          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" ./bin/apx supervisor &
          supervisor_pid=$!
          for _ in $(seq 1 50); do
            test -s "$fake_home/gateway.env" && break
            sleep 0.1
          done
          kill "$supervisor_pid" 2>/dev/null || true
          wait "$supervisor_pid" 2>/dev/null || true
          test -s "$fake_home/gateway.env"
          grep -qx 'APX_CAPTURE=full' "$fake_home/gateway.env"
          grep -qx 'APX_CAPTURE_FULL_ACK=i-understand' "$fake_home/gateway.env"
          grep -qx 'APX_CAPTURE_BODY_CAP=777' "$fake_home/gateway.env"
          grep -qx 'APX_REDACT_KEYS=internal_secret' "$fake_home/gateway.env"
          grep -qx "APX_METRICS_DB=$fake_home/.local/state/apx/custom.db" "$fake_home/gateway.env"
          grep -qx 'APX_METRICS_RETENTION_DAYS=9' "$fake_home/gateway.env"
          grep -qx 'APX_METRICS_BACKFILL=0' "$fake_home/gateway.env"
          grep -qx 'APX_MAX_REQUEST_BYTES=888' "$fake_home/gateway.env"
          grep -qx 'APX_DASHBOARD_TOKEN=secret-token' "$fake_home/gateway.env"

      - name: Gateway metrics dashboard API smoke
        run: |
          python - <<'PY'
          import json
          import os
          import sqlite3
          import subprocess
          import tempfile
          import threading
          import time
          import urllib.error
          import urllib.request
          from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer

          class Upstream(BaseHTTPRequestHandler):
              calls = 0
              def do_POST(self):
                  Upstream.calls += 1
                  n = int(self.headers.get("content-length", "0") or "0")
                  self.rfile.read(n)
                  body = json.dumps({
                      "id": "msg_1",
                      "usage": {
                          "input_tokens": 123,
                          "output_tokens": 45,
                          "cache_read_input_tokens": 10,
                          "cache_creation_input_tokens": 5,
                      },
                      "content": [{"type": "tool_use", "name": "demo"}],
                  }).encode()
                  self.send_response(200)
                  self.send_header("content-type", "application/json")
                  self.send_header("content-length", str(len(body)))
                  self.end_headers()
                  self.wfile.write(body)

              def log_message(self, *_args):
                  pass

          class ToolProxy(BaseHTTPRequestHandler):
              last_path = ""
              last_method = ""
              def do_GET(self):
                  self._reply()

              def do_POST(self):
                  self._reply()

              def _reply(self):
                  ToolProxy.last_path = self.path
                  ToolProxy.last_method = self.command
                  n = int(self.headers.get("content-length", "0") or "0")
                  self.rfile.read(n)
                  body = b"<html><script>fetch('/api/stats.json')</script></html>"
                  self.send_response(200)
                  self.send_header("content-type", "text/html; charset=utf-8")
                  self.send_header("content-length", str(len(body)))
                  self.end_headers()
                  self.wfile.write(body)

              def log_message(self, *_args):
                  pass

          def start_gateway(tmp, capture="metadata", ack="", token="", body_cap="", max_request_bytes="67108864", pxpipe_url=""):
              os.makedirs(os.path.join(tmp, "state"), exist_ok=True)
              os.makedirs(os.path.join(tmp, "config"), exist_ok=True)
              cfg = os.path.join(tmp, "config", "config.env")
              with open(cfg, "w", encoding="utf-8") as fh:
                  fh.write("\n".join([
                      "BIND_HOST=127.0.0.1",
                      "GATEWAY_PORT=18787",
                      "GATEWAY_ENABLED=1",
                      "APX_CHAIN=\"\"",
                      "HEADROOM_ENABLED=0",
                      "PXPIPE_ENABLED=0",
                      "SQUEEZR_ENABLED=0",
                      "",
                  ]))
              env = os.environ.copy()
              env.update({
                  "GATEWAY_TARGET_URL": "http://127.0.0.1:18788",
                  "GATEWAY_PORT": "18787",
                  "BIND_HOST": "127.0.0.1",
                  "APX_STATE_DIR": os.path.join(tmp, "state"),
                  "APX_CONFIG": cfg,
                  "APX_METRICS_DB": os.path.join(tmp, "state", "metrics.db"),
                  "APX_CAPTURE": capture,
                  "APX_CAPTURE_FULL_ACK": ack,
                  "APX_MAX_REQUEST_BYTES": max_request_bytes,
                  "APX_DASHBOARD_ENABLED": "1",
                  "APX_DASHBOARD_TOKEN": token,
              })
              if pxpipe_url:
                  env["APX_PXPIPE_URL"] = pxpipe_url
              if body_cap:
                  env["APX_CAPTURE_BODY_CAP"] = body_cap
              return subprocess.Popen(["python", "bin/apx-gateway"], env=env, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True)

          with tempfile.TemporaryDirectory() as tmp:
              refused = start_gateway(tmp, capture="full")
              refused.wait(timeout=5)
              assert refused.returncode == 3, refused.stdout.read()
          with tempfile.TemporaryDirectory() as tmp:
              refused = start_gateway(tmp, capture="metadata", body_cap="-1")
              refused.wait(timeout=5)
              assert refused.returncode == 3, refused.stdout.read()

          tool = ThreadingHTTPServer(("127.0.0.1", 18789), ToolProxy)
          threading.Thread(target=tool.serve_forever, daemon=True).start()
          with tempfile.TemporaryDirectory() as tmp:
              proc = start_gateway(tmp, token="secret-token", pxpipe_url="http://127.0.0.1:18789")
              try:
                  time.sleep(1)
                  try:
                      urllib.request.urlopen("http://127.0.0.1:18787/proxy/pxpipe/", timeout=5)
                      raise AssertionError("unauthorized proxy path unexpectedly succeeded")
                  except urllib.error.HTTPError as e:
                      assert e.code == 401, e
                  try:
                      req = urllib.request.Request(
                          "http://127.0.0.1:18787/proxy/pxpipe/api/toggle?token=secret-token&x=1",
                          data=b"{}",
                          headers={"content-type": "application/json"},
                          method="POST",
                      )
                      urllib.request.urlopen(req, timeout=5)
                      raise AssertionError("unauthorized proxy POST unexpectedly succeeded")
                  except urllib.error.HTTPError as e:
                      assert e.code == 401, e
                  opener = urllib.request.build_opener(urllib.request.HTTPCookieProcessor())
                  with opener.open("http://127.0.0.1:18787/?token=secret-token", timeout=5) as r:
                      assert "apx_dashboard_token" in r.headers.get("set-cookie", "")
                      r.read()
                  with opener.open("http://127.0.0.1:18787/api/capture", timeout=5) as r:
                      assert r.status == 200
                      r.read()
                  req = urllib.request.Request(
                      "http://127.0.0.1:18787/proxy/pxpipe/api/toggle?token=secret-token&x=1",
                      data=b"{}",
                      headers={"content-type": "application/json"},
                      method="POST",
                  )
                  with opener.open(req, timeout=5) as r:
                      html = r.read().decode("utf-8")
                      assert "/proxy/pxpipe/api/stats.json" in html
                  assert ToolProxy.last_method == "POST"
                  assert ToolProxy.last_path == "/api/toggle?x=1", ToolProxy.last_path
              finally:
                  proc.terminate()
                  try:
                      proc.wait(timeout=3)
                  except subprocess.TimeoutExpired:
                      proc.kill()
                  tool.shutdown()

          # Redaction must happen before capture truncation, otherwise key-based
          # secrets in large JSON bodies can leak when the truncated prefix is
          # not valid JSON.
          import runpy
          old_env = os.environ.copy()
          try:
              os.environ.update({
                  "APX_CAPTURE": "full",
                  "APX_CAPTURE_FULL_ACK": "i-understand",
                  "APX_CAPTURE_BODY_CAP": "28",
              })
              mod = runpy.run_path("bin/apx-gateway", run_name="apx_gateway_redaction_test")
              captured = mod["_capture_body"](
                  json.dumps({"password": "hunter2", "messages": [{"role": "user", "content": "x" * 200}]}).encode(),
                  "application/json",
              )
              assert "hunter2" not in captured, captured
              nested = {"a": {"b": {"c": {"d": {"e": {"f": {"g": {"h": {"i": {"api_key": "deep-secret"}}}}}}}}}}
              captured = mod["_capture_body"](json.dumps(nested).encode(), "application/json")
              assert "deep-secret" not in captured, captured
          finally:
              os.environ.clear()
              os.environ.update(old_env)

          upstream = ThreadingHTTPServer(("127.0.0.1", 18788), Upstream)
          threading.Thread(target=upstream.serve_forever, daemon=True).start()
          with tempfile.TemporaryDirectory() as tmp:
              proc = start_gateway(tmp, capture="metadata", max_request_bytes="16")
              try:
                  time.sleep(1)
                  before = Upstream.calls
                  req = urllib.request.Request(
                      "http://127.0.0.1:18787/v1/messages",
                      data=json.dumps({"model": "claude-sonnet-5", "messages": [{"role": "user", "content": "too large"}]}).encode(),
                      headers={"content-type": "application/json"},
                      method="POST",
                  )
                  try:
                      urllib.request.urlopen(req, timeout=5)
                      raise AssertionError("oversized body unexpectedly succeeded")
                  except urllib.error.HTTPError as e:
                      assert e.code == 413, e
                  assert Upstream.calls == before
              finally:
                  proc.terminate()
                  try:
                      proc.wait(timeout=3)
                  except subprocess.TimeoutExpired:
                      proc.kill()
          with tempfile.TemporaryDirectory() as tmp:
              proc = start_gateway(tmp, capture="metadata")
              db = os.path.join(tmp, "state", "metrics.db")
              try:
                  time.sleep(1)
                  for path in (
                      "/api/capture",
                      "/api/metrics/summary?window=1h",
                      "/api/metrics/timeseries?window=1h&bucket=1m",
                      "/api/metrics/sessions?window=24h",
                      "/api/tool/detect",
                      "/api/tool/compare",
                  ):
                      with urllib.request.urlopen("http://127.0.0.1:18787" + path, timeout=5) as r:
                          assert r.status == 200, path
                          r.read()
                  with urllib.request.urlopen("http://127.0.0.1:18787/", timeout=5) as r:
                      html = r.read().decode("utf-8")
                      assert "Tool Comparison" in html
                      assert "Squeezr iframe" in html

                  payload = {"model": "claude-sonnet-5", "messages": [{"role": "user", "content": "hello"}]}
                  req = urllib.request.Request(
                      "http://127.0.0.1:18787/v1/messages?api_key=supersecret",
                      data=json.dumps(payload).encode(),
                      headers={"content-type": "application/json"},
                      method="POST",
                  )
                  with urllib.request.urlopen(req, timeout=5) as r:
                      assert r.headers.get("x-apx-request-id")
                      assert r.headers.get("x-apx-session-id")
                      r.read()
                  time.sleep(0.3)
                  row = sqlite3.connect(db).execute(
                      "select status,tokens_in,tokens_out,cache_read_tokens,cache_write_tokens,tool_call_count,request_body,response_body,path from requests"
                  ).fetchone()
                  assert row[:6] == (200, 123, 45, 10, 5, 1), row
                  assert row[6] is None and row[7] is None, row
                  assert row[8].endswith("api_key=REDACTED"), row
              finally:
                  proc.terminate()
                  try:
                      proc.wait(timeout=3)
                  except subprocess.TimeoutExpired:
                      proc.kill()
                  upstream.shutdown()
          PY

      - name: Verify apx CLI help renders
        run: |
          chmod +x bin/apx
          ./bin/apx help

      - name: Verify apx target command rewrites derived routes
        run: |
          fake_home="$RUNNER_TEMP/apx-target-home"
          rm -rf "$fake_home"
          mkdir -p "$fake_home/.config/apx" "$fake_home/.local/state/apx"
          cp config/config.env "$fake_home/.config/apx/config.env"

          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx target set http://127.0.0.1:9999/anthropic --no-restart
          grep -qx 'APX_TARGET_API_URL="http://127.0.0.1:9999/anthropic"' "$fake_home/.config/apx/config.env"
          grep -qx 'GATEWAY_TARGET_API_URL="http://127.0.0.1:8788"' "$fake_home/.config/apx/config.env"
          grep -qx 'HEADROOM_TARGET_API_URL="http://127.0.0.1:9999/anthropic"' "$fake_home/.config/apx/config.env"

          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx chain set pxpipe,headroom --no-restart --no-claude-sync
          grep -qx 'GATEWAY_TARGET_API_URL="http://127.0.0.1:47821"' "$fake_home/.config/apx/config.env"
          grep -qx 'PXPIPE_TARGET_API_URL="http://127.0.0.1:8788"' "$fake_home/.config/apx/config.env"
          grep -qx 'HEADROOM_TARGET_API_URL="http://127.0.0.1:9999/anthropic"' "$fake_home/.config/apx/config.env"

          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx chain set headroom,squeezr --no-restart --no-claude-sync
          grep -qx 'HEADROOM_TARGET_API_URL="http://127.0.0.1:18780"' "$fake_home/.config/apx/config.env"
          grep -qx 'SQUEEZR_TARGET_API_URL="http://127.0.0.1:9999/anthropic"' "$fake_home/.config/apx/config.env"

          set +e
          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx target set ftp://example.com --no-restart >/tmp/apx-target-invalid.log 2>&1
          rc=$?
          set -e
          test "$rc" != 0
          set +e
          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx target set http://example.com:bad --no-restart >/tmp/apx-target-invalid-port.log 2>&1
          rc=$?
          set -e
          test "$rc" != 0
          set +e
          HOME="$fake_home" APX_CONFIG="$fake_home/.config/apx/config.env" APX_STATE="$fake_home/.local/state/apx" \
            ./bin/apx chain set headroom,headroom --no-restart --no-claude-sync >/tmp/apx-chain-dupe.log 2>&1
          rc=$?
          set -e
          test "$rc" != 0

      - name: Verify completions render for every shell
        run: |
          ./bin/apx completions bash > /tmp/apx.bash
          ./bin/apx completions zsh  > /tmp/apx.zsh
          ./bin/apx completions fish > /tmp/apx.fish
          test -s /tmp/apx.bash
          test -s /tmp/apx.zsh
          test -s /tmp/apx.fish

      - name: Verify install.sh --help renders
        run: ./install.sh --help

  # ── 2) Pack apx.sh on both Linux and macOS to catch tool differences ─────
  pack:
    name: Pack apx.sh (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Pack self-extracting apx.sh
        run: |
          bash build/pack.sh
          test -s dist/apx.sh
          test -s dist/apx.sh.sha256

      - name: Verify apx.sh --print-version matches VERSION file
        run: |
          embedded="$(bash dist/apx.sh --print-version)"
          file_version="$(head -n1 VERSION | tr -d '[:space:]')"
          test "$embedded" = "$file_version"

      - name: Verify apx.sh checksum
        run: |
          cd dist
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum -c apx.sh.sha256
          else
            # macOS lacks sha256sum by default; use shasum with the same file format.
            shasum -a 256 -c apx.sh.sha256
          fi

      - name: Verify apx.sh extracts a complete payload
        run: |
          extract_dir="$RUNNER_TEMP/apx-extract"
          rm -rf "$extract_dir"
          mkdir -p "$extract_dir"
          bash dist/apx.sh --extract-to "$extract_dir"
          test -x "$extract_dir/bin/apx"
          test -x "$extract_dir/bin/apx-gateway"
          test -x "$extract_dir/bin/apx-squeezr"
          test -f "$extract_dir/share/dashboard.html"
          test -f "$extract_dir/config/config.env"
          test -x "$extract_dir/install.sh"
          test -f "$extract_dir/VERSION"
          # Extracted VERSION must match embedded --print-version.
          extracted_version="$(head -n1 "$extract_dir/VERSION" | tr -d '[:space:]')"
          embedded="$(bash dist/apx.sh --print-version)"
          test "$extracted_version" = "$embedded"

      - name: Verify get.sh bootstrap round-trips against a real release
        run: |
          # Positive path: get.sh should fetch v0.2.0's apx.sh, verify its
          # sha256, and pass --print-version through to it.
          version=$(APX_VERSION=v0.2.0 bash get.sh --print-version 2>/dev/null | tail -1)
          test "$version" = "0.2.0"

      - name: Verify get.sh refuses a non-existent release
        run: |
          # Negative path: nonexistent tag → curl -f returns non-zero →
          # get.sh must exit non-zero before running any apx.sh.
          set +e
          APX_VERSION=v99.99.99 bash get.sh --print-version >/dev/null 2>&1
          rc=$?
          set -e
          test "$rc" != 0

      - name: Verify get.sh refuses a checksum mismatch
        run: |
          # Stub curl so get.sh receives an apx.sh whose bytes do not match
          # the published sha256. get.sh must refuse to execute.
          stub_dir="$RUNNER_TEMP/curl-stub"
          rm -rf "$stub_dir"; mkdir -p "$stub_dir"
          cat > "$stub_dir/curl" <<'SH'
          #!/usr/bin/env bash
          out=""; while [ $# -gt 0 ]; do case "$1" in -o) out="$2"; shift 2 ;; *) shift ;; esac; done
          # Serve a bogus apx.sh + a valid-looking (but mismatched) sha256.
          case "$out" in
            *apx.sh)         printf 'bogus\n' > "$out" ;;
            *apx.sh.sha256)  printf '%s  apx.sh\n' "$(printf 'expected\n' | sha256sum | awk '{print $1}')" > "$out" ;;
          esac
          exit 0
          SH
          chmod +x "$stub_dir/curl"
          set +e
          PATH="$stub_dir:$PATH" bash get.sh --print-version >/dev/null 2>&1
          rc=$?
          set -e
          test "$rc" != 0

      - name: Upload apx.sh artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v6
        with:
          name: apx-sh
          path: |
            dist/apx.sh
            dist/apx.sh.sha256
            dist/get.sh
            dist/get.sh.sha256
          if-no-files-found: error
          retention-days: 14

  # ── 3) End-to-end install + versioning smoke on macOS and Linux ─────────
  #      macOS is the primary target (LaunchAgent). Linux is best-effort:
  #      installs files but skips launchd.
  smoke:
    name: Smoke install (${{ matrix.os }})
    needs: pack
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    env:
      # Isolate the smoke install to a scratch HOME so we don't touch the runner's real one.
      FAKE_HOME: ${{ github.workspace }}/.smoke-home
    steps:
      - uses: actions/checkout@v5

      - name: Pack apx.sh
        run: bash build/pack.sh

      - name: Fresh install (release mode, no service, skip deps)
        run: |
          rm -rf "$FAKE_HOME"
          mkdir -p "$FAKE_HOME"
          HOME="$FAKE_HOME" bash dist/apx.sh --no-service --skip-deps

      - name: Verify layout after fresh install
        run: |
          test -L "$FAKE_HOME/.local/bin/apx"
          test -L "$FAKE_HOME/.local/bin/apx-gateway"
          test -L "$FAKE_HOME/.local/bin/apx-squeezr"
          test -L "$FAKE_HOME/.local/share/apx/current"
          test -f "$FAKE_HOME/.config/apx/install.mode"
          test -f "$FAKE_HOME/.config/apx/config.env"
          test "$(cat "$FAKE_HOME/.config/apx/install.mode")" = "release"
          # apx CLI works via the ~/.local/bin symlink
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" version
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" versions

      - name: Install a second version and verify current flip
        run: |
          orig_version="$(head -n1 VERSION | tr -d '[:space:]')"
          echo "${orig_version}-test" > VERSION
          bash build/pack.sh
          HOME="$FAKE_HOME" bash dist/apx.sh --no-service --skip-deps
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" versions
          # Restore VERSION for the next step
          echo "$orig_version" > VERSION

      - name: Rollback to the previous version
        run: |
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" rollback
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" versions

      - name: apx use / cleanup
        run: |
          orig_version="$(head -n1 VERSION | tr -d '[:space:]')"
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" use "v${orig_version}-test"
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" cleanup --keep 1 --dry-run
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" cleanup --keep 1
          # After cleanup with keep=1, only the current version should remain.
          count=$(ls "$FAKE_HOME/.local/share/apx/versions" | wc -l | tr -d ' ')
          test "$count" = "1"

      - name: Uninstall deps/caches uses manifest and preserves plain purge safety
        run: |
          stub_dir="$RUNNER_TEMP/apx-stubs"
          rm -rf "$stub_dir"
          mkdir -p "$stub_dir" \
                   "$FAKE_HOME/.local/state/apx" \
                   "$FAKE_HOME/.local/pipx/venvs/headroom-ai" \
                   "$FAKE_HOME/Library/Caches/headroom/bin" \
                   "$FAKE_HOME/.npm/_npx/px/node_modules/pxpipe-proxy" \
                   "$FAKE_HOME/.npm/_npx/sq/node_modules/squeezr-ai" \
                   "$FAKE_HOME/.npm/_npx/keep/node_modules/other-package" \
                   "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb" \
                   "$FAKE_HOME/.npm/_logs"
          touch "$FAKE_HOME/Library/Caches/headroom/bin/difft"
          touch "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/pxpipe"
          touch "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/squeezr"
          touch "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/other"
          touch "$FAKE_HOME/.npm/_logs/log"
          echo keep > "$FAKE_HOME/.npm/keep"

          cat > "$stub_dir/pipx" <<'SH'
          #!/usr/bin/env bash
          echo "$*" >> "$HOME/pipx.log"
          if [ "${1:-}" = "uninstall" ] && [ "${2:-}" = "headroom-ai" ]; then
            rm -rf "$HOME/.local/pipx/venvs/headroom-ai"
          fi
          if [ "${1:-}" = "uninject" ] && [ "${2:-}" = "headroom-ai" ] && [ "${3:-}" = "ast-grep-cli" ]; then
            echo "uninject-ok" >> "$HOME/pipx.log"
          fi
          exit 0
          SH
          chmod +x "$stub_dir/pipx"

          cat > "$FAKE_HOME/.local/state/apx/install-manifest.env" <<EOF
          APX_INSTALL_MANIFEST_VERSION=1
          HEADROOM_AI_INSTALLED=1
          AST_GREP_CLI_INJECTED=1
          HEADROOM_TOOLS_CACHE_DIR=$FAKE_HOME/Library/Caches/headroom/bin
          NPM_CACHE_DIR=$FAKE_HOME/.npm
          NPM_WARMED_PXPIPE_PROXY=1
          NPM_WARMED_SQUEEZR_AI=1
          NPM_PXPIPE_PROXY_CACHE_PATH=$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/pxpipe
          NPM_SQUEEZR_AI_CACHE_PATH=$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/squeezr
          EOF

          APX_PATH="$stub_dir:$FAKE_HOME/.local/bin:/usr/bin:/bin:/opt/homebrew/bin:/usr/local/bin" \
            HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge=deps,caches --dry-run
          test -d "$FAKE_HOME/.local/pipx/venvs/headroom-ai"
          test -d "$FAKE_HOME/Library/Caches/headroom/bin"
          test -d "$FAKE_HOME/.npm/_npx"
          test ! -f "$FAKE_HOME/pipx.log"

          if APX_PATH="$stub_dir:$FAKE_HOME/.local/bin:/usr/bin:/bin:/opt/homebrew/bin:/usr/local/bin" \
            HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge=bogus --dry-run; then
            echo "unknown purge category unexpectedly succeeded" >&2
            exit 1
          fi

          cat > "$FAKE_HOME/.local/state/apx/install-manifest.env" <<EOF
          APX_INSTALL_MANIFEST_VERSION=1
          AST_GREP_CLI_INJECTED=1
          EOF
          rm -f "$FAKE_HOME/pipx.log"
          APX_PATH="$stub_dir:$FAKE_HOME/.local/bin:/usr/bin:/bin:/opt/homebrew/bin:/usr/local/bin" \
            HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge=deps --yes
          grep -q "uninject headroom-ai ast-grep-cli" "$FAKE_HOME/pipx.log"
          ! grep -q "uninstall headroom-ai" "$FAKE_HOME/pipx.log"
          test -d "$FAKE_HOME/.local/pipx/venvs/headroom-ai"

          cat > "$FAKE_HOME/.local/state/apx/install-manifest.env" <<EOF
          APX_INSTALL_MANIFEST_VERSION=1
          HEADROOM_AI_INSTALLED=1
          AST_GREP_CLI_INJECTED=1
          HEADROOM_TOOLS_CACHE_DIR=$FAKE_HOME/Library/Caches/headroom/bin
          NPM_CACHE_DIR=$FAKE_HOME/.npm
          NPM_WARMED_PXPIPE_PROXY=1
          NPM_WARMED_SQUEEZR_AI=1
          NPM_PXPIPE_PROXY_CACHE_PATH=$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/pxpipe
          NPM_SQUEEZR_AI_CACHE_PATH=$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/squeezr
          EOF
          rm -f "$FAKE_HOME/pipx.log"
          APX_PATH="$stub_dir:$FAKE_HOME/.local/bin:/usr/bin:/bin:/opt/homebrew/bin:/usr/local/bin" \
            HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge=deps,caches --yes
          grep -q "uninstall headroom-ai" "$FAKE_HOME/pipx.log"
          ! grep -q "uninject headroom-ai" "$FAKE_HOME/pipx.log"
          test ! -d "$FAKE_HOME/.local/pipx/venvs/headroom-ai"
          test ! -d "$FAKE_HOME/Library/Caches/headroom/bin"
          test ! -d "$FAKE_HOME/.npm/_npx/px"
          test ! -d "$FAKE_HOME/.npm/_npx/sq"
          test -d "$FAKE_HOME/.npm/_npx/keep"
          test ! -f "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/pxpipe"
          test ! -f "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/squeezr"
          test -f "$FAKE_HOME/.npm/_cacache/content-v2/sha512/aa/bb/other"
          test -d "$FAKE_HOME/.npm/_logs"
          test -f "$FAKE_HOME/.npm/keep"
          test -L "$FAKE_HOME/.local/bin/apx"

          mkdir -p "$FAKE_HOME/.local/state/apx" "$FAKE_HOME/elsewhere/bin"
          rm -rf "$FAKE_HOME/Library/Caches/headroom"
          ln -s "$FAKE_HOME/elsewhere" "$FAKE_HOME/Library/Caches/headroom"
          cat > "$FAKE_HOME/.local/state/apx/install-manifest.env" <<EOF
          APX_INSTALL_MANIFEST_VERSION=1
          HEADROOM_TOOLS_CACHE_DIR=$FAKE_HOME/Library/Caches/headroom/bin
          EOF
          if APX_PATH="$stub_dir:$FAKE_HOME/.local/bin:/usr/bin:/bin:/opt/homebrew/bin:/usr/local/bin" \
            HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge=caches --yes; then
            echo "symlinked cache path unexpectedly succeeded" >&2
            exit 1
          fi
          test -d "$FAKE_HOME/elsewhere/bin"

      - name: Uninstall --purge cleans the versioned layout
        run: |
          mkdir -p "$FAKE_HOME/apx-src/.git"
          echo "$FAKE_HOME/apx-src" > "$FAKE_HOME/.config/apx/source.path"
          HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin/apx" uninstall --purge --yes
          test ! -e "$FAKE_HOME/.local/bin/apx"
          test ! -e "$FAKE_HOME/.local/share/apx/current"
          test ! -d "$FAKE_HOME/.local/share/apx/versions"
          test ! -f "$FAKE_HOME/.config/apx/install.mode"
          test -d "$FAKE_HOME/apx-src/.git"

      - name: Plain purge refuses to delete a recorded source clone inside apx roots
        run: |
          rm -rf "$FAKE_HOME"
          mkdir -p "$FAKE_HOME/.config/apx" "$FAKE_HOME/.local/share/apx/src/.git"
          cp bin/apx "$FAKE_HOME/.local/bin-apx"
          chmod +x "$FAKE_HOME/.local/bin-apx"
          echo "$FAKE_HOME/.local/share/apx/src" > "$FAKE_HOME/.config/apx/source.path"
          if HOME="$FAKE_HOME" "$FAKE_HOME/.local/bin-apx" uninstall --purge --yes; then
            echo "plain purge unexpectedly removed a root containing source clone" >&2
            exit 1
          fi
          test -d "$FAKE_HOME/.local/share/apx/src/.git"

      - name: Non-default APX_SHARE/APX_STATE/APX_CONFIG is refused by --purge
        run: |
          rm -rf "$FAKE_HOME"
          mkdir -p "$FAKE_HOME/Documents/precious" "$FAKE_HOME/.config/apx" "$FAKE_HOME/.local"
          touch "$FAKE_HOME/Documents/precious/keep.txt"
          cp bin/apx "$FAKE_HOME/.local/bin-apx"
          chmod +x "$FAKE_HOME/.local/bin-apx"
          set +e
          HOME="$FAKE_HOME" APX_SHARE="$FAKE_HOME/Documents/precious" \
            "$FAKE_HOME/.local/bin-apx" uninstall --purge=share --yes
          rc=$?
          set -e
          test "$rc" = 2
          test -f "$FAKE_HOME/Documents/precious/keep.txt"
          # APX_STATE / APX_CONFIG rejected the same way
          set +e
          HOME="$FAKE_HOME" APX_STATE="$FAKE_HOME/Documents/precious" \
            "$FAKE_HOME/.local/bin-apx" uninstall --purge=state --yes
          rc=$?
          set -e
          test "$rc" = 2

      - name: Invalid APX_LABEL is refused at load
        run: |
          rm -rf "$FAKE_HOME"
          mkdir -p "$FAKE_HOME/.local"
          cp bin/apx "$FAKE_HOME/.local/bin-apx"
          chmod +x "$FAKE_HOME/.local/bin-apx"
          set +e
          HOME="$FAKE_HOME" APX_LABEL='../../.zshrc' "$FAKE_HOME/.local/bin-apx" help
          rc=$?
          set -e
          test "$rc" = 2

  # ── 4) PR guard: if VERSION changed, require a CHANGELOG entry ──────────
  changelog:
    name: CHANGELOG entry present
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Check that VERSION bumps have a CHANGELOG entry
        run: |
          base="${{ github.event.pull_request.base.sha }}"
          head="${{ github.event.pull_request.head.sha }}"
          if git diff --name-only "$base" "$head" | grep -qx VERSION; then
            new_version="$(head -n1 VERSION | tr -d '[:space:]')"
            # Escape regex metacharacters in the version so `0.2.0` does not
            # accidentally match `0.2.0-rc1` (dots are regex "any char").
            esc="$(printf '%s' "$new_version" | sed 's/[.[\*^$()+?{|]/\\&/g')"
            # Anchor on the right side too: the heading must be exactly the
            # target version, optionally wrapped in [] and/or prefixed with v.
            if ! grep -Eq "^## \\[?v?${esc}\\]?([[:space:]]|\$)" CHANGELOG.md; then
              echo "VERSION bumped to $new_version but no CHANGELOG.md heading for it." >&2
              echo "Expected a line like '## [$new_version]' or '## v$new_version' or '## $new_version'." >&2
              exit 1
            fi
          else
            echo "VERSION unchanged; skipping CHANGELOG heading check."
          fi
